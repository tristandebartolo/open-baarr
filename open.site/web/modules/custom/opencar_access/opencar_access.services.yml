services:
  opencar_access.baseline.permissions:
    class: Drupal\opencar_access\EventSubscriber\BaselineRedirect
    arguments: ['@config.factory', '@current_user']
    tags:
      - { name: event_subscriber }
