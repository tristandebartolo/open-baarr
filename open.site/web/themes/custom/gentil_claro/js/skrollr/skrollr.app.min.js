/**
 * @file
 * gentil's intence image.
 */

(($, Drupal, once) => {
  Drupal.behaviors.gmSkrollr = {
    attach(context) {
      once("gmSkrollrInit", "body", context).forEach((value) => {

        var s = skrollr.init({
          smoothScrolling: true,
          smoothScrollingDuration: 1500,
          render: function (data) {
            //Debugging - Log the current scroll position.
            console.log(data.curTop);
          },
          edgeStrategy: "set",
          easing: {
            WTF: Math.random,
            inverted: function (p) {
              return 1 - p;
            },
          },
        });

        skrollr.menu.init(s, {
          easing: "outCubic",
          animate: true,
          duration: 3000,
        });
      });
    },
  };
})(jQuery, Drupal, once);
